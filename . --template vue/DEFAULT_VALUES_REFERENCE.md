# 快速参�?- 默认值映射表

## 当产品字段缺失时使用的默认�?

### 字符串字�?
| 字段 | 默认�?| 说明 |
|------|--------|------|
| name | 'N/A' | 产品名称 |
| origin | 'Unknown' | 起始�?/ 厂家地址 |
| density | '1.0' | 密度系数（支�?0.8"�?0.8g/cm³"格式�?|

### 数值字�?- 比率
| 字段 | 默认�?| 说明 | 百分�?|
|------|--------|------|--------|
| vat_rate | 0.13 | 增值税�?| 13% |
| refund_rate | 0.09 | 退税率 | 9% |

### 数值字�?- 价格/成本
| 字段 | 默认�?| 说明 | 单位 |
|------|--------|------|------|
| price_per_ton | 5000 | 吨价 | RMB |
| custom_pkg_price | 100 | 定制包装单价 | RMB |
| box_unit_price | 50 | 箱单�?| RMB |
| plate_fee | 200 | 版费 | RMB |

### 数值字�?- 数量
| 字段 | 默认�?| 说明 |
|------|--------|------|
| custom_pkg_moq | 1000 | 定制模式起订�?|
| units_per_box | 1000 | 每箱单位�?|

### 对象字段
| 字段 | 默认�?| 说明 |
|------|--------|------|
| shipping_fees | {} | 各港口运费（空对象） |

---

## 示例

### 完全缺失数据的产�?
```javascript
{
  name: 'Test Product with Missing Data',
  price_per_ton: null,
  density: null,
  vat_rate: null
  // ... 其他字段都缺�?
}
```

被处理为�?
```javascript
{
  name: 'Test Product with Missing Data',  // 有值，保留原�?
  price_per_ton: 5000,                      // null �?默认�?5000
  density: '1.0',                           // null �?默认�?'1.0'
  vat_rate: 0.13,                          // null �?默认�?0.13
  // ... 其他字段都用默认值补�?
}
```

### 部分缺失数据的产�?
```javascript
{
  name: 'Partial Data Product',
  price_per_ton: 4500,     // 有�?
  density: '0.8g/cm³',     // 有�?
  vat_rate: null,          // 缺失
  origin: null,            // 缺失
}
```

被处理为�?
```javascript
{
  name: 'Partial Data Product',
  price_per_ton: 4500,     // 保留
  density: '0.8g/cm³',     // 保留
  vat_rate: 0.13,          // null �?默认 0.13
  origin: 'Unknown',       // null �?默认 'Unknown'
}
```

---

## 验证命令

### 查看当前产品数量
```bash
node -e "const fs = require('fs'); const data = JSON.parse(fs.readFileSync('data.json', 'utf8')); console.log('Products:', data.products.length);"
```

### 测试缺失数据的产品计�?
```bash
node test-missing-data.js
```

### 重新构建应用
```bash
npm run build-nosign
```

### 运行应用
```bash
npx electron dist-electron/main.js
```

---

## 故障排除

| 问题 | 可能原因 | 解决方案 |
|------|--------|--------|
| 产品不显�?| 产品名称为空 | 检�?Excel �?data.json 中的产品名称 |
| 计算结果�?0 | 港口运费未设�?| 添加 shipping_fees 对象或使用自定义港口 |
| UI 显示 'Unknown' | 字段缺失 | 正常行为，系统使用了默认�?|
| 应用崩溃 | 产品数据不正�?| 清空 data.json 并重启应用重新加�?Excel |

---

## 注意事项

1. **用户可以覆盖默认�?* - UI 中的所有输入字段都可以编辑
2. **默认值是合理�?* - 都基于行业标准和之前的实际使用数�?
3. **保存后生�?* - 用户输入的值会保存�?data.json
4. **��ʹ�� data.json ��Ϊ��Ʒ����Դ

